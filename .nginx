server {
    listen 80;
    listen [::]:80;
    location ~ ^((?!api).)*$ {
        root /var/app;
        try_files $uri /index.html =404;
    }
    location / {
        proxy_pass http://backend-backend:8080;
    }
}